/* This program displays the interactive mode to the user */
#include "shell.h"
#include <stdio.h>
/**
* _prompt - prints prompt for user to enter input
*/
void _prompt(void)
{
	fprintf(stderr, "$ ");
}
